const obj = {};

obj.push("pat");

console.log(obj);

// What will happen when this file is executed with NodeJS ? type error
// What is the problem with this code ? you can not push on an object its array method
